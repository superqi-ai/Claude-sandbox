# Secrets — never commit these
.env
config/gmail_credentials.json
config/gmail_token.json

# Downloaded transcripts (temporary, cleaned up after each run)
downloads/

# Cached Claude analysis results (may contain PII)
cache/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# OS
.DS_Store
Thumbs.db
